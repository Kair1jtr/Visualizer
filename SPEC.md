# ヘキサうどん ルール概要 & API仕様まとめ

第37回全国高等専門学校プログラミングコンテスト 競技部門「ヘキサうどん」の
ルールと、本リポジトリが提供する API 仕様をまとめたもの。本リポジトリには
3系統の API がある。

- **本番試合観戦API**（`/api/real/*`。第3章）: 配布された公式簡易サーバー
  `procon-server` をサブプロセスとして起動し、その実際のHTTP APIをそのまま
  中継・要約するもの。公式サーバーの正式な通信仕様は
  [docs/model.md](docs/model.md) にまとめている。
- **シミュレーター観戦API**（`/api/sim/*`。第4章）: 公式ルール忠実
  シミュレーター（`simulator/`）で1試合を走らせ、**`/api/real/status` と
  同じ形**で結果を返すもの。同じ設定JSONを実サーバー版と読み比べられる。
- **旧モックAPI**（`/api/match` など。第2章）: 本リポジトリ独自のモック実装。
  シミュレーター観戦APIの登場前に使っていたもので、**公式ルールを完全には
  再現していない**（燃料不足の移動を「補給されるまで待機」として実行する。
  公式は回答全体をリジェクトする〔要項〕〔Q6〕）。既存の依存があるため残置。

> 旧モックAPIのパス (`/api/...`) は本リポジトリ独自の仮のもの。
> **レスポンス/リクエストの中身**は公式フォーマット資料に準拠している。

---

## 1. ルール概要

### 1.1 フィールド

- 六角形グリッド（**pointy-top**、**even-r オフセット**配置）。**偶数行**が右に半セルずれる
  （公式Q&Aその1 Q1/A1 で確定）。
- セル番号は行優先で `0 〜 (width×height − 1)`（`pos = y × width + x`）。
  募集要項 p.8 図1「マップの座標例」の 0〜99 の並びと一致する。
- 地形は4種類（`map.cells` の値）:

  | コード | 地形 | 説明 |
  |---|---|---|
  | 0 | 平地 | 通行可能 |
  | 1 | 道路 | 通行可能。混雑度により速度が変化 |
  | 2 | 山地 | 通行可能。コスト高 |
  | 3 | 池 | **進入不可** |

### 1.2 移動コスト（出発セルの地形で決定）

移動に必要な「ステップ数」と「消費燃料」は、**移動を開始するセルの地形**によって決まる（道着セルではない）。

| 地形 | ステップ数 | 消費燃料 |
|---|---|---|
| 平地 | 2 | 1 |
| 山地 | 3 | 2 |
| 道路（順調） | 1 | 2 |
| 道路（混雑） | 2 | 2 |
| 道路（渋滞） | 4 | 2 |
| 池 | 進入不可 | — |

- 燃料を消費するのは**巡回車のみ**（補給車は燃料の概念なし）。
- 燃料切れ、あるいは残りステップ数が移動に必要なステップ数に満たない場合は待機する。

### 1.3 方向コード（0〜5）

行動計画で使う移動方向は、隣接6方向を時計回りに番号付けしたもの。

| コード | 方向 |
|---|---|
| 0 | 左上 (NW) |
| 1 | 右上 (NE) |
| 2 | 右 (E) |
| 3 | 右下 (SE) |
| 4 | 左下 (SW) |
| 5 | 左 (W) |

行動計画の値は以下のいずれか:
- **-1 以下**: 待機（`-N` は N ステップ連続の待機を表す圧縮表現）
- **0〜5**: 上表の方向へ1セル移動

### 1.4 エージェント種別

| コード | 種別 | 役割 |
|---|---|---|
| 0 | 巡回車 | 移動して各地のスポットからうどんを獲得する。燃料を消費する |
| 1 | 補給車 | 燃料の概念なし。巡回車に帯同して燃料を補給する |

- 1チームのエージェント数は **3〜8機**（ルール上の範囲）。
- 補給: 巡回車と補給車が**同一セルに1ステップ以上滞在**すると、巡回車の燃料が上限 (`fuelLimits`) まで満タンになる。

### 1.5 スポット（うどん獲得）

- 各スポットは `brand`（うどんの系列）と `pos`（セル位置）、`stocks`（1日あたりの在庫上限）を持つ。
- 獲得ルール:
  - **1台の巡回車が1スポットから1日1玉**まで獲得できる。
  - 在庫は**チームごとに独立**（他チームの獲得は自チームの在庫に影響しない）。
  - 在庫は**毎日 `stocks` まで補充**される。
  - 獲得は「到着した瞬間」に限らない。反映フェーズの3番目で**現在地がスポットなら**判定される
    ため、日をまたいでスポット上に留まっていても、その日の1ステップ目以降に獲得できる
    〔Q7〕〔Q8〕（〔補足〕巡回車Eの例）。0ステップ目は反映フェーズが無いので獲得しない。

### 1.6 道路の混雑状態

道路セルの状態（順調・混雑・渋滞）は、**前日+前々日の全チームの当該セル滞在ステップ数の合計をチーム数で割った値**を閾値と比較して決まる（初日はすべて順調）。

| 状態コード | 状態 | 条件 |
|---|---|---|
| 0 | 順調 | 合計滞在数/チーム数 < `busyThreshold` |
| 1 | 混雑 | `busyThreshold` ≤ 値 < `jammedThreshold` |
| 2 | 渋滞 | 値 ≥ `jammedThreshold` |

`busyThreshold` / `jammedThreshold` は試合ごとに `match` フォーマットで通知される。

### 1.7 勝敗判定

日々の試合終了後、以下の優先順位でチームを順位付けする:

1. **うどんの種類数**（全日を通して獲得した `brand` の種類の総数）が多い順
2. **日ごとの種類数の累積**（毎日ごとにその日獲得した種類数を数え、全日分を合計した値）が多い順
3. **獲得した玉の総数**が多い順
4. （回答時間 — 本モックサーバーではシミュレーション対象外）

---

## 2. 旧モックAPI仕様（本リポジトリ独自のモック、公式フォーマット準拠）

> **注意**: このモックは公式ルールを完全には再現していない。
> 特に燃料不足の移動を「補給されるまで待機」として実行するが、公式は
> **回答全体をリジェクト**する〔要項〕〔Q6〕。新規の検討には第4章の
> シミュレーター観戦API（`/api/sim/*`）を使うこと。

すべて **HTTP/1.1** で応答する（本番競技サーバーの制約に合わせ、`uvicorn` を `h11` バックエンドで起動）。

### 2.1 `GET /api/match`

試合開始前の**マップ構成フォーマット**を返す。

```jsonc
{
  "startsAt": 1234567890,        // 試合開始時刻（UNIX epoch秒）
  "daySeconds": [7, 5, 10, ...], // 各日の行動計画 回答受付時間（秒）
  "daySteps": [40, 44, ...],     // 各日の合計ステップ数
  "map": {
    "width": 12, "height": 10,
    "cells": [[0,1,2,...], ...]  // [row][col] = 地形コード（0平地/1道路/2山地/3池）
  },
  "spots": [
    { "brand": 0, "pos": 27, "stocks": 2 }, ...
  ],
  "agents": [10, 18, 30, 109],   // 各エージェントの初期セル位置
  "fuelLimits": 14,              // 燃料上限
  "players": 3,                  // 参加チーム数
  "busyThreshold": 4,            // 道路「混雑」判定の閾値
  "jammedThreshold": 8           // 道路「渋滞」判定の閾値
}
```

### 2.2 `GET /api/match/{day}`

各日開始時の**試合情報フォーマット**（自チーム=チーム0視点）を返す。`day` は 0 始まり。

```jsonc
{
  "endsAt": 1234567890,   // この日の行動計画 回答締切時刻
  "day": 0,               // 現在の日（0始まり）
  "agents": [             // 自チームのエージェント状態
    { "kind": 0, "pos": 15, "fuel": 14 }, ...
  ],
  "others": [             // 他チームのエージェント状態
    { "id": 1, "agents": [ { "kind": 0, "pos": 15, "fuel": 14 }, ... ] }, ...
  ],
  "traffics": [           // 道路セルの現在の混雑状態
    { "pos": 7, "status": 0 }, ...   // status: 0順調/1混雑/2渋滞
  ]
}
```

### 2.3 `POST /api/agents`

**エージェント種別の回答**を送信・検証する。

- リクエストボディ: `[0, 1, 0, 1]` のような整数配列（0=巡回車, 1=補給車）
- 検証内容:
  - 配列の要素数がエージェント数と一致すること
  - 各値が `0` か `1` のみであること
- レスポンス: `{ "accepted": true }`（不正時は `422`）

### 2.4 `POST /api/actions?day=N`

**行動計画の回答**を送信・検証する（`day` クエリパラメータ、省略時 `0`）。

- リクエストボディ: エージェントごとの行動列の配列。例 `[[3,2,-4], [0,1,-3,5], ...]`
  - `-N`: N ステップ待機
  - `0〜5`: 上記の方向コードへ1セル移動
- 検証内容:
  - 行動列の本数がエージェント数と一致すること
  - 各値が整数で、`-1以下` または `0〜5` の範囲であること
  - 各エージェントのステップ合計（待機は絶対値、移動は地形・道路状態依存のステップ数）が、その日の `daySteps` と一致すること
  - 移動先が盤外・池でないこと
- レスポンス: `{ "accepted": true }`（不正時は `422`、理由付き）
- ※ フォーマット面の検証のみ行い、燃料切れの判定までは行わない。

### 2.5 `POST /api/new`

新しいサンプル試合を生成し、現在の試合として保持する（本リポジトリ独自の補助エンドポイント）。
ライブ対戦中に呼ぶとライブ試合は破棄され、サンプルモードに戻る。

| クエリパラメータ | 範囲 | デフォルト |
|---|---|---|
| `seed` | 任意の整数（省略でランダム） | - |
| `teams` | 2〜8 | 3 |
| `days` | 4〜10 | 5 |
| `agents` | 3〜8 | 4 |
| `width` | 8〜32 | 12 |
| `height` | 8〜32 | 10 |

レスポンス: `{ "ok": true, "seed": 42 }`

### 2.6 `GET /api/replay`

ビジュアライザ用に、試合データ一式（公式フォーマット + 補助情報）をまとめて返す（本リポジトリ独自）。構造は README.md の「ビジュアライザ用データ形式」を参照。ライブ対戦中は完了した日までの途中経過を返す。

### 2.7 ライブ対戦モード（本リポジトリ独自）

`POST /api/actions` を「検証のみ」から「実際の試合進行」に変えるモード。チーム0=クライアント（プレイヤー）。既定の `teams=1` では**他チームを一切生成しない1人プレイ**（他プレイヤーとは無関係に自分の巡回のみ）。`teams` を2以上にすると内蔵の貪欲AIチームが加わり順位を競う。

| メソッド/パス | 内容 |
|---|---|
| `POST /api/live/new?teams=1&…` | ライブ対戦を開始（`teams` 以外のクエリパラメータは `/api/new` と同じ。`teams` の既定値は `1`、範囲は 1〜8） |
| `GET /api/live` | 進行状況: `status`（`waiting_agents` / `waiting_actions` / `finished`）、受付中の `day`、`solo`（1人プレイか）、成績/暫定順位 `standings` |

進行フロー:

1. `POST /api/live/new` → マップ生成、種別回答待ち
2. `GET /api/match` → マップ構成を取得
3. `POST /api/agents` → チーム0の種別が確定し試合開始（再提出は `409`）
4. `GET /api/match/{day}` → 当日の試合情報（未来の日は `409`）
5. `POST /api/actions?day={day}` → 検証(不正は `422`)後、**その日を実行**して翌日へ。レスポンスに `finished` と `standings` を含む
6. 4〜5 を最終日まで繰り返し → `finished: true`

実行時の扱い（フォーマット検証を通過した計画のみ実行される）:

- 燃料が足りない移動は**補給されるまで待機**し、計画は保留される（そのぶん計画末尾が日内に収まらなければ切り捨て）
- 日内に完了しないステップ数の移動は待機扱い
- 実際に実行された行動が `days[].plans` に記録されるため、ビジュアライザのリプレイと完全に一致する

### 2.8 `GET /debug`, `/debug/day/{n}`, `/debug/requests`

サーバー内部の状態を確認するデバッグ GUI（本リポジトリ独自、FastAPI + Jinja2）。ライブ対戦中は進行状況カードも表示。詳細は README.md を参照。

### 2.9 サンプルクライアント

- `examples/client.py` — ライブ対戦の通信手順（試合開始→種別提出→毎日の情報取得と
  行動計画提出）を実装した対戦クライアントの例。

> 以前ここにあった `algorithm/`（行動計画テンプレート・プランビルダー）は
> 現在リポジトリに存在しない。行動計画を組み立てる参照実装としては
> `simulator/strategy.py`（貪欲法）と `simulator/pathfinding.py`（その日の
> 道路状態を反映した Dijkstra）を参照のこと。

```bash
python app.py                        # サーバー起動（別ターミナル）
python examples/client.py --seed 42  # 対戦実行
```

> `examples/client.py` は削除済みの `algorithm/` に依存しているため、
> 現状そのままでは動かない。

### 2.10 観戦専用ビュー (`/watch.html`)

`POST /api/new`（サンプルモード生成）や `POST /api/live/new` 以降のライブ対戦
進行を**自動検知して、手動操作なしで最初から最後まで自動再生する**観戦専用
ページ（本リポジトリ独自、`static/js/watch.js`）。生成・アップロードなどの
操作系ボタンは持たない。

- 2秒間隔で `GET /api/replay` をポーリングし、`meta.seed` / `meta.generator` /
  完了日数 (`days.length`) から識別子を作って前回と比較する
- 識別子が変化（新しい試合が生成された、またはライブ対戦の日が進んだ）したら
  自動的に読み込み直し、最初から自動再生を開始する
- `days` が空（ライブ対戦でまだ1日も完了していない）の間は待機表示のまま
- 最終日の最終ステップまで再生し終えたら自動停止し、「次の試合を待っています」
  と表示して次のポーリングを継続する
- 別プロセス・別タブ・`curl` などから試合が生成/進行しても、開いたままの
  タブが自動的に追従する（観戦用モニターとして表示しっぱなしにする用途向け）

---

## 3. 本番試合観戦（`procon-server` サブプロセス）

配布された公式簡易サーバーのバイナリ（`server/簡易サーバー/procon-server-*`）
を、実行中のOS/CPUに合ったものを選んでこのアプリがサブプロセスとして起動・
停止する。試合そのものの進行（開始時刻・締切・実行）は `procon-server` 自身が
すべて内部で管理するため、本リポジトリ側は「起動・停止」と「観戦（ポーリング）」
だけを担当する。

### 3.1 `POST /api/real/start?config=<path>`

`procon-server` を起動する。`config` は試合設定JSONのパス（省略時は配布サンプル
`server/試合設定用JSONファイル/example.json`）。空いているポートを自動選択し、
`-kind-deadline 5s -match-start-delay 5s` で起動する。設定JSON内の1チーム目の
トークンを使って観戦を開始する。既に起動中の場合は `409`。

### 3.2 `POST /api/real/stop`

`procon-server` を停止する（起動していなければ何もしない）。

### 3.3 `GET /api/real/status`

観戦データを返す。主なフィールド:

| フィールド | 内容 |
|---|---|
| `phase` | `"connecting"` / `"waiting"`（種別受付・開始待ち）/ `"running"` / `"ended"` |
| `setting` | `procon-server` の `GET /setting` レスポンス（マップ・スポット等） |
| `teams` | チームID・名前の一覧 |
| `days[]` | 日ごとのスナップショット。`agentsByTeam`（日開始時点の位置・燃料）、
  `traffics`（道路状態）、`trajectories`（推定移動軌跡。翌日のスナップショットが
  無い場合は `null`） |

`trajectories` は前日・翌日の2スナップショット間を、その日の地形・道路状況を
コストとした Dijkstra 最短経路で結んだ**推定値**であり、公式APIが提供する
実データではない（実際の経路と一致するとは限らない）。同じ理由で、**試合の
最終日だけは軌跡を計算できない**（比較対象の「翌日のスナップショット」が
存在しないため）。

また `procon-server` は試合終了後、すべてのエンドポイントを `403`
（`{"code":"AccessTimeError","message":"match has ended"}`）で拒否するため、
**最終スコアや在庫などの結果は公式APIから一切取得できない**。観戦中に取得
できたスナップショットのみが `days[]` に残り続ける。

### 3.4 観戦ビュー (`/`, `static/index.html`)

上記3エンドポイントをポーリングするだけの**閲覧専用**ページ（本リポジトリ
独自、`static/js/realmatch.js`）。トップページ `/` として配信される。
起動・停止ボタンはサブプロセスの管理用で、
試合そのものへの操作（行動計画の提出など）はできない。日タブで過去の日の
盤面を見返せる。

右側の「チーム」欄でチームを選ぶと、盤面下の「推測経路」欄に、そのチームが
保有する各車両（巡回車・補給車）ごとに、その日の出発点・到達点・通過セルの
座標列（`(列,行)` 表記）を一覧表示する。一覧の行をクリックすると、その車両の
推測経路だけが盤面上に半透明の線でハイライト表示される（既定では軌跡線は
表示されず、選択したときだけ表示する）。

---

## 4. シミュレーター観戦（`simulator/` で1試合を実行）

公式ルール忠実シミュレーターで1試合を最後まで走らせ、`/api/real/status` と
**同じ形**の観戦データを返す。実時間の締切が無いため試合は一瞬で終わる。

`procon-server` に渡すのと同じ試合設定JSON（`server/試合設定用JSONファイル/`）を
読むので、同一設定の試合を「実サーバーで動かした結果」と「シミュレーターで
動かした結果」で見比べられる。

### 4.1 `POST /api/sim/start?config=<path>&strategy=<name>`

| クエリ | 既定 | 内容 |
|---|---|---|
| `config` | `server/試合設定用JSONファイル/example.json` | 試合設定JSONのパス |
| `strategy` | `greedy` | 行動計画を作る戦略。`greedy` / `stay`。チームごとに変えるならカンマ区切り（例: `greedy,stay`） |

レスポンス: `{"ok": true, "days": <実行した日数>}`。
設定JSONが読めない・戦略名が不正な場合は `422`。

### 4.2 `POST /api/sim/stop`

実行結果を破棄する。レスポンス: `{"ok": true}`

### 4.3 `GET /api/sim/status`

未実行なら `{"running": false, "started": false}`。実行後は `/api/real/status`
と同じキー（`setting` / `teams` / `days` / `currentDay` / `numDays` / `phase` ほか）に
加えて、シミュレーター固有の情報を返す。

| キー | 内容 |
|---|---|
| `exact` | 常に `true`。軌跡が推定ではなく実測であることを示す |
| `strategy` | 使用した戦略 |
| `policies` | 採用中の仮仕様（U-1/U-3/U-4/U-5/U-6）の一覧 |
| `ranking` | 勝敗判定順に並べたチーム一覧 |
| `days[].numSteps` | その日のステップ数 N |
| `days[].steps[]` | `{step, agentsByTeam}` を 0〜N の全ステップ分。ステップ再生に使う |
| `days[].scores[]` | その日終了時点の得点（①種類数 ②日ごと種類数の累積 ③玉数） |
| `days[].volumes` | 道路セルごとの交通量（その日の道路状態を決めた値） |
| `days[].stay` | その日に発生した滞在ステップ数（全チーム合算） |
| `days[].plans` | 実際に採用された行動計画 |
| `days[].rejected` | チームごとのリジェクト理由（有効なら `null`） |
| `setting.key` | 実行ごとに変わる識別子。ブラウザ側が盤面を作り直すかの判定に使う |

`days[].trajectories` は `/api/real/status` と同じ形だが、中身は Dijkstra による
推定ではなく、全ステップの位置を畳んだ**実測**の通過セル列である。

### 4.4 観戦ビュー (`/sim.html`, `static/sim.html`)

本番試合観戦 (`/`) と同じ描画コード（`static/js/matchview.js`）を使う。
ステップスライダー・再生ボタン・得点欄・仮仕様欄がこのページにだけある。

---

## 5. 参考: FastAPI 自動ドキュメント

http://127.0.0.1:8000/docs でこれらのエンドポイントの対話的なドキュメントを確認できる。
