# ヘキサうどん 問題モデル整理

アルゴリズム設計の前提として、競技「ヘキサうどん」を**プログラムが扱える形**に
整理したもの。チーム全員の共通認識用。

## 出典と信頼度

| 記号 | 意味 | 出典 |
|---|---|---|
| ✅**API** | 公式API仕様書で確定 | `server/回答システムに関する情報/redoc-static.html`（Procon 37 API, OpenAPI 3.1.0） |
| ✅**設定** | 公式の試合設定JSON仕様で確定 | `server/試合設定用JSONファイル/試合設定JSONについて.md` |
| ✅**書式** | 公式フォーマット資料で確定 | 「競技部門『ヘキサうどん』のフォーマットについて」 |
| ✅**Q&A** | 公式Q&Aで確定 | `docs/第37回大会競技部門問い合わせと回答その１.pdf`〜`その２.pdf`・`その3.pdf`・`_補足資料(行動詳細)-1.pdf` |
| ⚠️**要再確認** | 募集要項由来として本リポジトリに記録されているが、原典の再確認を推奨 | `SPEC.md` / `visualizer/pathfinding.py`（「表1」由来のコメントあり） |
| ❌**未確定** | どの資料にも明記が見つかっていない | — |

> **注意**: 公式API仕様書は通信面のみを規定しており、移動コスト・補給・得点計算などの
> **ゲームルール本体は含まれていない**。⚠️の項目は募集要項で必ず裏取りすること。

---

## ① 入力として何が与えられるか

### A. 試合設定（試合開始前に1回）✅**API** `GET /setting` → `MatchSetting`

| キー | 型 | 意味 | 範囲・備考 |
|---|---|---|---|
| `startsAt` | int | 試合開始時刻(UNIX time) | **確定前は `0`** が返る ✅API |
| `daySeconds` | int[] | 各日の**回答時間(秒)** | 要素数=日数 |
| `daySteps` | int[] | 各日のステップ数 | **`W+H` 〜 `(W+H)×4`** ✅API |
| `map.height` / `map.width` | int | 盤面 H / W | |
| `map.cells` | int[H][W] | 地形 | `0`平地 `1`道路 `2`山地 `3`池 ✅設定 |
| `spots[]` | `{brand, pos, stocks}` | スポット | brand=系列, stocks=最大在庫量 |
| `agents[]` | int[] | 自分のエージェント初期位置 | **この順序が以降すべての基準** ✅書式 |
| `fuelLimits` | int | 燃料積載量上限 | **1日目ステップ数の1〜3倍** ✅API |
| `players` | int | 参加競技者数 | |
| `busyThreshold` | int | 混雑基準値 | **1〜5** ✅API |
| `jammedThreshold` | int | 渋滞基準値 | **2〜10** ✅API |

### B. 試合状態（各日の開始時）✅**API** `GET /` → `MatchState`

| キー | 意味 |
|---|---|
| `endsAt` | その日の**回答受付終了時刻**(UNIX time) |
| `day` | 日番号（初日は `0`） |
| `agents[]` | 自分のエージェント `{kind, pos, fuel}`。kind `0`巡回車 `1`補給車 |
| `others[]` | 他プレイヤー `{id, agents[]}` — **他チームの位置・燃料も全部見える** |
| `traffics[]` | `{pos, status}` status `0`順調 `1`混雑 `2`渋滞 |

### 座標系 ✅設定 / ✅Q&A

セル番号は**行優先の1次元インデックス**。

```
pos = y * width + x
x   = pos % width
y   = pos / width
```

盤面は pointy-top の **even-r offset**（**偶数行**が右に半セルずれる）hex グリッド。
`試合設定JSONについて.md`には行優先インデックスの説明のみでオフセット方向の記載は
無いが、Q&Aその1 Q1/A1で明確に確定している:

> Q1. 募集要項の図1の例で、セル0,1,2が所属する行を0行目とした場合、六角形の敷き詰め方は
> 偶数行が右にずれる地形で固定されているか
> A1. 偶数行が右にずれる形で固定されています。

（過去、本リポジトリは奇数行オフセット(odd-r)で実装しており誤りだった。
`visualizer/hexgrid.py` / `static/js/hex.js` で修正済み。`simulator/grid.py` は
最初から even-r）

---

## ② 出力として何を出すのか

### A. エージェント種別（試合開始前に1回）✅**API** `POST /agent`

```json
[0, 1, 0, 1]
```
`0`=巡回車, `1`=補給車。**要素数はエージェント数と一致必須**。

### B. 行動計画（毎日1回）✅**API** `POST /`

```json
[
  [-15],
  [0, 1, -10]
]
```

- 外側の配列＝エージェント（`setting.agents` と同じ順序）、内側＝そのエージェントの行動列
- `-1以下` = 待機（絶対値がステップ数）
- `0〜5` = 移動方向

| コード | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 方向 | 左上 | 右上 | 右 | 右下 | 左下 | 左 |

**レスポンス** `ActionAccepted.revision` ✅API:
- `0未満` = 提出失敗 / `0以上` = 受理
- **同じ日に複数回提出でき、`revision` が大きいものがより後に受理された提出**

> 💡 これは重要。**回答時間内なら何度でも出し直せる**ので、
> 「まず安全な計画を即座に提出 → 時間が余る限り改善版を再提出」という
> anytime アルゴリズムが有効（⑦参照）。

### 認証 ✅API

`Procon-Token` **ヘッダ**、または `token` **クエリパラメータ**でトークンを渡す。

---

## ③ 1回の操作で何が変化するか

### 構造的性質

これはリアルタイムゲームではなく、**1日分の行動列をまとめて事前提出する**方式。
解くべきは「1日分の行動列を決める最適化問題 × 日数」であり、
**日の途中で他チームの動きに反応することはできない**（日の開始時点の情報のみ）。

### 移動1回で起きること

| 変化 | 内容 |
|---|---|
| 消費ステップ | ⚠️ **出発セル（現在地）の地形**で決まる。到着先ではない ← 最も間違えやすい |
| 消費燃料 | 同じく出発セルの地形。⚠️ **巡回車のみ**消費、補給車は消費しない |
| 位置 | 消費ステップ数が経過して初めて到着（移動中は複数ステップ拘束される） |

**コスト表** ⚠️要再確認（募集要項 表1 由来。`visualizer/pathfinding.py:12`）

| 地形 | ステップ | 燃料 |
|---|---|---|
| 平地 | 2 | 1 |
| 山地 | 3 | 2 |
| 道路（順調） | 1 | 2 |
| 道路（混雑） | 2 | 2 |
| 道路（渋滞） | 4 | 2 |
| 池 | 進入不可 | — |

### 移動以外の状態変化 ✅Q&A

- **獲得**: 巡回車のいるセルがスポットで、在庫>0 かつ「その巡回車がその日そのスポット未取得」なら1玉獲得。
  **到着した瞬間に限らない**——日をまたいでスポット上に留まっている場合も、その日の
  1ステップ目以降（＝最初の反映フェーズ以降）に獲得できる（Q&Aその1 Q7/A7・Q8/A8）。
  ただしスポットは必ず平地（移動コスト2ステップ）に置かれるため、1ステップで
  そのセルを離れることはできず、日をまたいでの居座りと通常の到着は区別なく扱ってよい
  （補足資料 巡回車E の例）
- **補給**: 巡回車と補給車が**同一セルに1ステップ以上一緒に留まる**と燃料が `fuelLimits` まで回復。
  判定は反映フェーズの「燃料補給」のタイミング（＝移動反映後）で同一セルにいたかどうかで行う。
  そのため同時に同じセルへ移動してきた場合は、移動反映の順序（先に処理された側が先に位置確定）
  次第で補給が成立しないケースがある（Q&Aその2 Q22/A22）
- **交通量の記録**: **移動反映後**のセルへの滞在ステップ数が累積され、翌日以降の道路状態に反映
  （Q&Aその2 Q27/A27）
- **在庫**: チームごとに独立（他チームが取っても自分の在庫は減らない）。毎日 `stocks` まで補充
- **同時到着の優先順位**: `agents` 配列のインデックス順（若い方が優先）（Q&Aその2 Q26/A26）

### 道路状態の決定 ✅Q&A

```
volume = (前日 + 前々日の全チーム滞在ステップ数) ÷ 参加チーム数
volume >= jammedThreshold → 渋滞(2)
volume >= busyThreshold   → 混雑(1)
それ以外                   → 順調(0)
```
初日は全て順調。2日目は前日（1日目）の交通量のみを使用（前々日が存在しないため）。

### 状態遷移の実装モデル

1日をシミュレートするのに必要な状態:

```
各エージェント: 位置 / 燃料 / 移動中の残ステップ / 本日獲得済みスポット集合
チーム単位:     スポット在庫 / 獲得済み系列集合 / 本日獲得系列集合 / 玉数
盤面:           道路セルごとの滞在ステップ累積（翌日の道路状態用）
```

公式の反映フェーズ順序（✅Q&A、Q&Aその1 Q6/A6・補足資料で確定）:

```
1. 燃料の消費（出発セルの地形で決定。巡回車のみ）
2. 移動の反映
3. うどんの獲得（現在地がスポットなら）
4. 燃料の補給（補給車と同一セルなら）
5. 交通量の更新（道路セルへの滞在をカウント）
```
各日の0ステップ目はアクションフェーズのみ（反映なし）、最終ステップは反映フェーズのみ
（アクションなし）。`simulator/engine.py` の `reflection_phase()` / `action_phase()` が
この順序をそのままコードの構造にしている。

---

## ④ 操作できる回数・時間

| 制約 | 値 |
|---|---|
| 提出回数 | 種別1回 + 行動計画を1日1回（ただし**同じ日に再提出可**、最後の受理が有効）✅API |
| 1回の思考時間 | `daySeconds[day]` 秒 |
| 締切 | `endsAt`（絶対時刻）。通信往復も込みで間に合わせる |
| 1日の行動量 | `daySteps[day]` ステップ（`W+H` 〜 `(W+H)×4`）✅Q&A（Q20/A20） |
| 種別提出の締切 | 簡易サーバーでは起動から既定 `5s`（`-kind-deadline`）、その後 `5s`（`-match-start-delay`）で試合開始 ✅簡易サーバーREADME |

**公式example**（`server/試合設定用JSONファイル/example.json`）: 10×10、`daySteps=[20,20,20,20]`、`daySeconds=[30,30,30,30]`、`fuelLimits=20`、エージェント3機、2チーム。

---

## ⑤ 勝敗・得点の決まり方 ✅Q&A

優先順位:

1. **うどんの種類数** — 全日程で獲得した `brand` の異なり数
2. **日ごとの種類数の累積** — 各日「その日獲った種類数」を全日分合計
3. **玉の総数**
4. 回答時間 — **各日の最後の有効な回答が送られるまでに要した時間の合計**（少ない方が勝ち）。
   計測の厳密な開始・終了点は資料からは確定できていない（Q&Aその2 Q56/A56）

**戦略的含意**:
- 玉数より**系列の網羅が最優先**
- ②があるため「**早い日に多種類を揃える**」ほど累積が伸びる
  （最終日に全種類揃えても累積は1日分しか稼げない）
- → 評価関数は「新規系列 > 本日未取得系列 > 既取得系列」の重み付けが自然

---

## ⑥ 絶対にしてはいけない操作（リジェクト条件）

### 公式に明記されている不正 ✅API / ✅書式

| 対象 | 不正条件 |
|---|---|
| 種別 | 要素数がエージェント数と不一致 / `{0,1}` 以外の値 |
| 行動計画 | **合計ステップ数がその日のステップ数と不一致** |
| 行動計画 | **移動できない方向への移動**（池・マップ外） |
| 行動計画 | 🔴 **必要燃料が不足する移動** ✅API |

### 特に重要な2点

- 🔴 **1つでも不正な行動が含まれる場合、全エージェントの行動計画を不正として扱う** ✅API
  （部分採用はされない）
- **リジェクト／時間切れ時のデフォルト** ✅書式
  - 種別 → **全エージェントが巡回車**になる
  - 行動計画 → **全エージェントが1日待機**（= その日を丸ごと失う、致命的）

### 実装上の必須対策

**提出前にローカルで完全検証する**こと。特に**燃料チェックは公式で不正条件なので必須**。
検証すべき項目:

1. 配列の要素数 == エージェント数
2. 各値が `-1以下` または `0〜5`
3. ステップ合計 == `daySteps[day]`（出発セル地形＋その日の道路状態で計算）
4. 全移動先が盤内かつ池でない
5. **各移動時点で燃料が足りている**（巡回車のみ、補給による回復も加味）

---

## ⑦ 問題の最大サイズと探索空間

| 項目 | 値 | 出典 |
|---|---|---|
| `daySteps` | `W+H` 〜 `(W+H)×4` | ✅Q&A（Q20/A20） |
| `fuelLimits` | 1日目ステップ数の1〜3倍の**範囲内**（整数倍限定ではない） | ✅Q&A（Q14/A14・Q60/A60・Q62/A62） |
| `busyThreshold` | 1〜5の正整数 | ✅Q&A（Q13/A13・Q30/A30） |
| `jammedThreshold` | 2〜10の正整数 | ✅Q&A（Q13/A13・Q30/A30） |
| マップ W / H | ❌未確定（本リポジトリのモックは 8〜32 を仮定） | ❌ |
| エージェント数 | ❌未確定（モックは 3〜8 を仮定。公式exampleは3機） | ❌ |
| 日数 | ❌未確定（`daySteps` の配列長。モックは 4〜10、公式exampleは4日） | ❌ |
| 参加者数 | 公式exampleは2、フォーマット資料の例は8 | — |

### 探索空間の見積り

W=H=32 とすると `daySteps` は最大 `(32+32)×4 = 256`。
1エージェントの1日の行動列は、平地移動(2ステップ)想定で最大128手 × 各7択（6方向+待機）
≈ **7¹²⁸ ≈ 10¹⁰⁸**。エージェント数分の組み合わせまで考えると当然全探索は不可能。

→ **全探索によるオラクル（正解判定器）は極小ケース専用**として作る。目安:

| 用途 | 盤面 | エージェント | daySteps | 日数 | 概算 |
|---|---|---|---|---|---|
| オラクル | 4×4 | 1〜2機 | 6〜10 | 1〜2日 | 7⁵ ≈ 1.7万通り |
| 中規模検証 | 6×6 | 2機 | 12〜24 | 2日 | ビームサーチ等の比較用 |

### 時間設計（`daySeconds` への適合）

`revision` による**再提出が可能**なので、次の設計が有効:

```
1. 締切に対して十分早い段階で「必ず妥当な計画」を作って即提出（保険）
2. 残り時間で探索を深め、改善案ができるたびに再提出
3. 締切の手前で打ち切り（通信往復のマージンを確保）
```

これにより「探索が間に合わず1日全損」という最悪ケースを構造的に回避できる。

---

## 未確定事項（要調査）

`docs/`に追加された公式Q&A（その1〜3・補足資料）で、以下は確定済み:

| # | 項目 | 状況 |
|---|---|---|
| 1 | 移動コスト表（平地2/山地3/道路1,2,4、燃料1,2,2） | ✅確定（Q&Aその1 Q10/A10・Q11/A11、補足資料） |
| 2 | 補給の成立条件の細部 | ✅確定（Q&Aその2 Q22/A22）。同時到着（すれ違い）では補給が成立しないケースがある |
| 3 | 獲得の発火条件（到着時のみか、滞在中も発火するか） | ✅確定（Q&Aその1 Q7/A7・Q8/A8）。到着時に限らず、日をまたいだ滞在でも1ステップ目以降に獲得できる |
| 4 | 道路状態の決定式（前日+前々日 ÷ チーム数） | ✅確定（Q&Aその2 Q27/A27）。移動反映後のセルをカウント |
| 5 | 得点・勝敗の正確な定義 | ✅ほぼ確定（Q&Aその2 Q56/A56）。回答時間の厳密な計測開始・終了点のみ未確定 |
| 6 | エージェント数の範囲 | ✅確定: **1チームあたり**3〜8体（Q&Aその2 Q50/A50） |
| 6' | マップサイズ・日数の範囲 | ❌未確定（今回のQ&Aにも明記なし。本リポジトリのモックは W/H=8〜32、日数=4〜10と仮定） |
| 7 | 同一セルへの複数エージェント同時進入の可否 | ✅確定: 可能（補足資料の例でセル0に巡回車4台が同時配置されている） |

> 💡 実際の`server/簡易サーバー/`バイナリで極小の設定JSONを使って状態遷移を
> 直接観測する手法も、Q&Aで判断が付かない細部（例: 燃料補給の1ステップ判定の
> 境界ケース）の検証には引き続き有効。

---

## 本リポジトリの実装との差分

競技ルールの実装は `simulator/` の1か所だけである（`docs/ルール説明書.md` /
`docs/状態設計書.md` に対応）。公式ルールとの差分は無く、公式資料だけでは
決まらない項目のみ `simulator/policies.py` に切り替え可能な形で隔離している
（U-1 セル番号 / U-3 交通量の除算 / U-4 2日目の除数 / U-5 反映順序 /
U-6 燃料消費タイミング）。

以前併存していたモックサーバー（`visualizer/hexaudon.py` と `/api/match` 系の
独自エンドポイント）は**削除済み**。公式と異なる意味論（燃料不足を
「補給されるまで待機」として実行する）を持っていたため、二重管理をやめた。

### 未実装（ルールではなく入出力の話）

| 箇所 | 現状 | 公式仕様 |
|---|---|---|
| 対戦クライアント | 未作成。`/api/real/*` は**観戦のみ**（`GET /setting` と `GET /` だけを叩く） | `POST /agent` / `POST /` で実際に指す |
| 認証 | 観戦では `Procon-Token` ヘッダを使用済み | 同じ |
| 再提出 | 未実装 | `revision` により再提出可。回答時間内なら何度でも出し直せる（⑦の anytime 設計を参照） |
| 回答時間の計測 | `simulator/` では外部から与える値として扱う | 計測基準が公式資料で未確定（Q56） |
